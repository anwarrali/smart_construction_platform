import { useCallback, useEffect, useState } from "react";
import { AlertTriangle, Bot, CheckCircle2, ClipboardList, Eye, RefreshCw, ShieldAlert, Sparkles, Target } from "lucide-react";
import { useNavigate, useParams } from "react-router-dom";
import toast from "react-hot-toast";

import { Badge } from "../../../components/ui/Badge";
import { Button } from "../../../components/ui/Button";
import { Card } from "../../../components/ui/Card";
import { useRole } from "../../../hooks/useRole";
import api from "../../../services/api";
import type { AIInsight, AIIntelligenceOverview } from "../../../types/aiInsight";
import { labelize, LoadingState } from "../../ifc/components/IFCShared";
import { useProjectWorkspace } from "../../projects/context/ProjectWorkspaceContext";
import { AIInsightSources } from "../components/AIInsightSources";

const severityVariant=(severity:string)=>severity==="CRITICAL"?"danger":severity==="WARNING"?"warning":severity==="ATTENTION"?"info":"neutral";
const selectClass="rounded-md border bg-background px-3 py-2 text-sm";

export const AIIntelligencePage=()=>{
  const workspace=useProjectWorkspace();const params=useParams<{projectId?:string}>();const projectId=workspace.projectId||params.projectId;const navigate=useNavigate();const {role}=useRole();
  const [overview,setOverview]=useState<AIIntelligenceOverview>();const [items,setItems]=useState<AIInsight[]>([]);const [loading,setLoading]=useState(true);const [busy,setBusy]=useState("");
  const [filters,setFilters]=useState({status:"",severity:"",category:""});
  const load=useCallback(async()=>{if(!projectId)return;setLoading(true);try{const [summary,insights]=await Promise.all([api.aiIntelligence.overview(projectId),api.aiIntelligence.insights(projectId,filters)]);setOverview(summary);setItems(insights);}catch{toast.error("Project intelligence could not be loaded.");}finally{setLoading(false);}},[filters,projectId]);
  useEffect(()=>{void load();},[load]);
  const run=async()=>{if(!projectId)return;setBusy("run");try{await api.aiIntelligence.run(projectId,overview?.latestRevision?.id);toast.success("Structured project intelligence refreshed");await load();}catch{toast.error("Intelligence analysis could not be completed.");}finally{setBusy("");}};
  const action=async(id:string,operation:()=>Promise<unknown>,message:string)=>{setBusy(id);try{await operation();toast.success(message);await load();}catch(error){const detail=(error as {response?:{data?:{detail?:string}}}).response?.data?.detail;toast.error(detail||"The review action could not be completed.");}finally{setBusy("");}};
  const open3D=(item:AIInsight)=>{const ids=item.affectedJson?.elements||[];navigate(`${workspace.path("ifc")}?tab=viewer&elementIds=${encodeURIComponent(ids.join(","))}`);};
  if(!projectId)return <Card>Select a project to open AI Intelligence.</Card>;
  if(loading&&!overview)return <LoadingState label="Building project intelligence…"/>;
  const alignment=overview?.alignment;
  return <div className="page-container space-y-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="flex items-center gap-2 text-sm font-semibold text-primary"><Sparkles size={16}/> Structured construction analytics</p><h1 className="mt-1 text-2xl font-bold">AI Intelligence</h1><p className="mt-1 text-sm text-muted-foreground">Persistent, evidence-backed analysis connecting the IFC model to tasks, issues, revisions, and field evidence.</p></div><Button disabled={busy==="run"} onClick={()=>void run()}><RefreshCw className={busy==="run"?"animate-spin":""} size={16}/> Refresh analysis</Button></div>
    <Card className="border-amber-200 bg-amber-50/40"><div className="flex gap-3"><ShieldAlert className="text-amber-700"/><div><b>Automated analysis — engineering review required.</b><p className="text-sm text-muted-foreground">Insights never approve engineering work or change project state automatically.</p></div></div></Card>
    <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-6"><Card><p className="text-xs text-muted-foreground">Open insights</p><p className="mt-2 text-3xl font-bold">{overview?.openInsights||0}</p></Card><Card><p className="text-xs text-muted-foreground">Critical</p><p className="mt-2 text-3xl font-bold text-red-600">{overview?.severityCounts.CRITICAL||0}</p></Card><Card><p className="text-xs text-muted-foreground">Warnings</p><p className="mt-2 text-3xl font-bold text-amber-600">{overview?.severityCounts.WARNING||0}</p></Card><Card><p className="text-xs text-muted-foreground">Suggestions</p><p className="mt-2 text-3xl font-bold">{overview?.severityCounts.SUGGESTION||0}</p></Card><Card className="xl:col-span-2"><p className="text-xs text-muted-foreground">IFC ↔ Project alignment</p><div className="mt-2 flex items-end gap-3"><p className="text-3xl font-bold">{alignment?.overall??0}%</p><Badge variant={(alignment?.overall||0)<50?"warning":"success"}>{(alignment?.overall||0)<50?"Review recommended":"Aligned"}</Badge></div><p className="mt-2 text-xs text-muted-foreground">Latest revision: {overview?.latestRevision?.revisionCode||overview?.latestRevision?.title||"Unavailable"}</p></Card></div>
    {alignment&&<Card><div className="flex items-center gap-2"><Target/><h2 className="font-semibold">Transparent alignment score</h2></div><p className="mt-1 text-sm text-muted-foreground">Weighted structured metrics—no unexplained AI score.</p><div className="mt-4 grid gap-4 md:grid-cols-2 xl:grid-cols-5">{Object.entries(alignment.components).map(([key,value])=><div key={key}><div className="flex justify-between text-xs"><span>{labelize(key)}</span><b>{value}%</b></div><div className="mt-2 h-2 rounded bg-muted"><div className="h-full rounded bg-primary" style={{width:`${Math.min(100,value)}%`}}/></div><p className="mt-1 text-[11px] text-muted-foreground">Weight {alignment.weights[key]}%</p></div>)}</div><details className="mt-4 rounded border p-3 text-sm"><summary className="cursor-pointer font-medium">Calculation evidence</summary><div className="mt-3 grid gap-2 sm:grid-cols-2"><p>IFC disciplines: {Object.entries(alignment.ifcDisciplines).map(([key,value])=>`${labelize(key)} ${value}`).join(" · ")||"None"}</p><p>Task disciplines: {Object.entries(alignment.taskDisciplines).map(([key,value])=>`${labelize(key)} ${value}`).join(" · ")||"None"}</p><p>Linked tasks: {alignment.linkedTaskCount} / {alignment.taskCount}</p><p>Linked issues: {alignment.linkedIssueCount} / {alignment.issueCount}</p></div></details></Card>}
    <Card><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-semibold">Review queue</h2><p className="text-sm text-muted-foreground">Filter persistent alerts and suggestions by review state and source area.</p></div><div className="flex flex-wrap gap-2"><select className={selectClass} value={filters.status} onChange={e=>setFilters(v=>({...v,status:e.target.value}))}><option value="">All statuses</option>{["NEW","UNDER_REVIEW","CONFIRMED","DISMISSED","RESOLVED"].map(v=><option key={v}>{v}</option>)}</select><select className={selectClass} value={filters.severity} onChange={e=>setFilters(v=>({...v,severity:e.target.value}))}><option value="">All severities</option>{["CRITICAL","WARNING","ATTENTION","SUGGESTION","INFORMATION"].map(v=><option key={v}>{v}</option>)}</select><select className={selectClass} value={filters.category} onChange={e=>setFilters(v=>({...v,category:e.target.value}))}><option value="">All categories</option>{["MODEL","TASKS","SUGGESTED_TASK","EVIDENCE","REVISION"].map(v=><option key={v}>{v}</option>)}</select></div></div></Card>
    {!items.length?<Card><div className="py-10 text-center"><Bot className="mx-auto text-muted-foreground"/><p className="mt-3 font-semibold">No insights match these filters</p><p className="text-sm text-muted-foreground">Refresh analysis after a processed IFC revision becomes available.</p></div></Card>:<div className="grid gap-4 xl:grid-cols-2">{items.map(item=><Card key={item.id} className={item.severity==="CRITICAL"?"border-red-300":item.severity==="WARNING"?"border-amber-300":""}><div className="flex items-start justify-between gap-3"><div className="flex gap-3">{item.category==="SUGGESTED_TASK"?<ClipboardList className="mt-1 text-primary"/>:<AlertTriangle className="mt-1 text-amber-600"/>}<div><div className="flex flex-wrap gap-2"><Badge variant={severityVariant(item.severity)}>{labelize(item.severity)}</Badge><Badge>{labelize(item.category)}</Badge><Badge variant="info">{Math.round(item.confidence*100)}% confidence</Badge></div><h3 className="mt-3 text-lg font-bold">{item.title}</h3></div></div><Badge variant={item.status==="NEW"?"warning":item.status==="RESOLVED"?"success":"neutral"}>{labelize(item.status)}</Badge></div><p className="mt-4 text-sm leading-6">{item.description}</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="rounded-lg bg-muted/40 p-3"><p className="text-xs font-semibold">Why this was detected</p><p className="mt-1 text-xs text-muted-foreground">{item.reason}</p></div><div className="rounded-lg bg-primary/5 p-3"><p className="text-xs font-semibold">Recommended action</p><p className="mt-1 text-xs text-muted-foreground">{item.recommendedAction}</p></div></div><details className="mt-3 rounded border p-3 text-xs"><summary className="cursor-pointer font-medium">Source evidence</summary><pre className="mt-2 max-h-48 overflow-auto whitespace-pre-wrap">{JSON.stringify(item.evidenceJson,null,2)}</pre></details><div className="mt-4 flex flex-wrap gap-2">{!!item.affectedJson?.elements?.length&&<Button size="sm" variant="outline" onClick={()=>open3D(item)}><Eye size={15}/> View in 3D</Button>}{item.status==="NEW"&&<Button size="sm" variant="outline" disabled={busy===item.id} onClick={()=>void action(item.id,()=>api.aiIntelligence.review(projectId,item.id,"UNDER_REVIEW"),"Insight moved to review")}>Review</Button>}<Button size="sm" variant="outline" disabled={busy===item.id} onClick={()=>void action(item.id,()=>api.aiIntelligence.review(projectId,item.id,"DISMISSED"),"Insight dismissed")}>Dismiss</Button><Button size="sm" variant="outline" disabled={busy===item.id} onClick={()=>void action(item.id,()=>api.aiIntelligence.review(projectId,item.id,"RESOLVED"),"Insight resolved")}><CheckCircle2 size={15}/> Resolve</Button>{item.category==="SUGGESTED_TASK"&&role==="project_manager"&&!item.appliedEntityId&&<Button size="sm" disabled={busy===item.id} onClick={()=>void action(item.id,()=>api.aiIntelligence.createTask(projectId,item.id),"Task created for review")}>Create Task</Button>}{item.category!=="SUGGESTED_TASK"&&!item.appliedEntityId&&["project_manager","engineer"].includes(role||"")&&<Button size="sm" disabled={busy===item.id} onClick={()=>void action(item.id,()=>api.aiIntelligence.createIssue(projectId,item.id),"Issue created")}>Create Issue</Button>}</div></Card>)}</div>}
    {!!items.length&&<Card><h2 className="font-semibold">Traceable insight sources</h2><p className="text-sm text-muted-foreground">Open an insight below to see the current validity of every project record used.</p><div className="mt-3 space-y-2">{items.map(item=><div key={item.id}><p className="text-xs font-medium">{item.title}</p><AIInsightSources projectId={projectId} insightId={item.id} fallback={item.evidenceJson}/></div>)}</div></Card>}
  </div>;
};
