fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1
python-dotenv==1.0.0
python-jose[cryptography]==3.3.0
passlib[argon2]
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator
openai>=2.0.0,<3.0.0
httpx>=0.27.0,<0.28.0
ifcopenshell==0.8.5
# RAG: PDF text extraction and token-aware chunking. Both are optional at
# import time — the application starts and every non-RAG test runs without
# them; RAG reports itself unavailable instead of erroring.
pypdf==5.1.0
tiktoken==0.8.0
# The SQLAlchemy column type for pgvector's `vector`, and the adapter that
# sends a Python list as one. Required at import time by
# `models/document_chunk.py`, so it is a hard dependency rather than an
# optional one like pypdf: the model must load whether or not RAG is enabled.
pgvector==0.3.6
