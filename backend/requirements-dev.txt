# Test-only dependencies. Kept out of requirements.txt so the runtime image
# stays free of test tooling; install inside the running backend container with:
#   docker compose exec backend pip install -r requirements-dev.txt
-r requirements.txt
pytest==8.3.4
